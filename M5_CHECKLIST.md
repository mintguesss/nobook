# M5 實機驗證檢查表

規格 §11 M5：「前面全部是模擬。這一項必須真的去上一堂課。
M5 才是這個專案的真正驗收，前面的閘門只是確保你不會帶著已知缺陷走進教室。」

分兩階段：**先在家乾跑**（10 分鐘，能攔掉大部分問題），確認沒問題再進教室。

---

## 階段 0：Tailscale 設定（一次性）

目前筆電上**還沒安裝 Tailscale**，這是整條行動裝置路徑的前置條件。

```powershell
winget install tailscale.tailscale
```

然後：

1. 筆電與手機各自登入**同一個 Tailscale 帳號**
2. 到 Tailscale 管理後台的 DNS 頁面啟用 **HTTPS Certificates**
3. 筆電上執行：

```powershell
python -m server.main                                    # 先起服務
tailscale serve --bg --https=443 http://localhost:8000   # 再對外
tailscale status                                         # 記下機器名稱
```

4. 手機瀏覽器開 `https://<機器名>.<tailnet>.ts.net`

**必須走 HTTPS。** `getUserMedia` 只在 secure context 下可用，HTTPS 頁面連 `ws://`
會被 mixed content 擋掉。不要用自簽憑證——Android Chrome 對自簽憑證的
WebSocket 支援很差（規格 §8.3）。

---

## 階段 1：在家乾跑（10 分鐘）

對著手機講 10 分鐘的話就好，內容不重要。目的是把「只有真實裝置才會暴露的問題」
一次找出來。

- [ ] 頁面能開啟，課程下拉選單有東西（代表 `/api/courses` 通了）
- [ ] 按「開始錄音」→ 瀏覽器跳出麥克風權限請求 → 允許
- [ ] **逐字稿真的開始出現**（這一步驗證了 HTTPS + wss + AudioWorklet 整條路）
- [ ] 頂部指示燈是綠色，RTF／佇列／VRAM 有數字在跳
- [ ] 螢幕**沒有**自動變暗（wake lock 生效）
- [ ] 講滿 40 秒後按「標記重點」→ 摘要在 10 秒內出現
- [ ] 長按按鈕 → 註解輸入框展開，打字後摘要帶著註解
- [ ] 手動往上捲逐字稿 → 出現「回到最新」浮動按鈕 → 點了會跳回底部
- [ ] 點摘要上的時間戳 → 逐字稿跳到對應位置並高亮
- [ ] **切到別的 App 再切回** → 跳出「錄音中斷了 N 秒」提示，逐字稿裡有黃色缺漏標記
- [ ] **把 Wi-Fi 關掉 20 秒再開** → 指示燈轉黃再轉綠，跳出「已補送 N 秒錄音」
- [ ] 按「結束課程」→ 出現完整筆記 → 「複製 Markdown」與「下載 .md」都能用

任何一項失敗就先別去上課，回報是哪一項。

### 乾跑時特別要注意的

**來電**。PWA 沒有 foreground service，來電會蓋掉畫面、凍結頁面、停掉錄音。
這段音訊是真的消失，不像斷線那樣能靠本地 buffer 補送。
建議上課前**開飛航模式再單獨開 Wi-Fi**。

---

## 階段 2：真實課堂

### 上課前

- [ ] 筆電開機、服務啟動、`tailscale serve` 已生效（可先用手機開一次頁面確認）
- [ ] 手機電量足夠，接行動電源更保險（錄音 + 螢幕常亮很耗電）
- [ ] 飛航模式 + Wi-Fi
- [ ] 為這門課建一個 `courses/<id>.yaml`，術語表填該科目的專有名詞
      —— 實測 `initial_prompt` 能讓術語命中率 **+18% 到 +46%**，這是最划算的一步
- [ ] 麥克風擺位：離講者越近越好

### 上課中

- [ ] 手機平放桌上，螢幕朝上（不要蓋住麥克風）
- [ ] 老師講完一個段落就按一次「標記重點」
- [ ] 偶爾瞄一眼指示燈是不是綠的

### 下課後

- [ ] 按「結束課程」，等完整筆記生成（會切換到 8B 模型，約需 10–20 秒冷啟動）
- [ ] 下載 Markdown

---

## 驗收標準（規格 §11 M5）

- [ ] 平板／手機連線穩定、wake lock 有效、全程未斷
- [ ] 逐字稿準確度主觀可接受（抽 3 段人工比對，字錯率概估 < 15%）
- [ ] 按鈕在盲按情況下好按
- [ ] **產出的筆記真的有用**

最後一項是整個專案的真正驗收，沒有任何自動化測試能取代。

---

## 已知會發生的事

先講清楚，免得當成 bug：

**來電或切走 App 會造成缺漏。** 畫面上會標出來，但那段音訊救不回來。

**內建麥克風會產生英文幻覺。** 實測平板內建麥克風的逐字稿出現
`Good.` `So I mean.` `This is not.` 這類憑空冒出的英文；手機內建較好但仍非理想。
規格 §12 說收音品質是「對準確度影響最大的單一變因，超過任何模型選擇」，
指向性或領夾式麥克風是唯一的真正解法。

**下課後的環境雜音會產生幻覺文字。** VAD 會過濾掉大部分，但講話聲交雜的場合
仍可能吐出無意義內容。記得下課就按「結束課程」。

**教室 Wi-Fi 可能擋掉 Tailscale。** 備案是手機熱點（規格 §12）。
但如果手機同時當錄音裝置又當熱點，等於沒有備案——這種情況只能改用平板錄音。
